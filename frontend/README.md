# Frontend (Vite + React)

Run locally:

```bash
cd frontend
npm install
npm run dev
```

App runs on Vite (port 5173 by default). The frontend expects a backend API base URL in `VITE_API_URL` (e.g., `http://localhost:5000`).
